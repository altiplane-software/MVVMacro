import Foundation
import MVVMacroImpl
import MVVMacroAPI

// This is a placeholder executable target that helps build the macro implementation
// into a binary artifact. The actual XCFramework building happens in the build script.

print("MVVMacro Binary Builder")
print("This tool is used to build the MVVMacroBinary XCFramework")
print("To build the XCFramework, run the build_xcframework.sh script")